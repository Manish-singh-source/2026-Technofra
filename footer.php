 <footer class="twc-footer-wrap">
     <div class="twc-footer-container container">

         <div class="twc-footer-top">
             <div class="twc-top-grid">

                 <div class="twc-about-col">
                     <div class="twc-brand-logo">
                         <a href="https://technofra.com/">
                             <img src="assets\image\icons\technofra_logo.png" alt="Technofra Logo" width="200">
                         </a>
                     </div>

                     <p class="twc-about-text">
                         Technofra helps businesses unlock their online potential with innovative web design and
                         development solutions.
                     </p>

                     <p class="twc-about-text">
                         We create strong digital experiences that grow your brand and deliver real results.
                     </p>
                 </div>

                 <div class="twc-news-col-1">
                     <h3 class="twc-footer-title">Services</h3>
                     <ul class="twc-news-list">
                         <li><a href="web-design"><i class="fa-solid fa-angle-right"></i>Website Development</a>
                         </li>
                         <li><a href="ios-app-development"><i class="fa-solid fa-angle-right"></i>App
                                 Development</a></li>
                         <li><a href="shopify-website-development"><i class="fa-solid fa-angle-right"></i>E-Commerce
                                 Development</a></li>
                         <li><a href="branding"><i class="fa-solid fa-angle-right"></i>Branding</a></li>
                         <li><a href="digital-marketing"><i class="fa-solid fa-angle-right"></i>Digital
                                 Marketing</a></li>
                         <li><a href="social-media-marketing"><i class="fa-solid fa-angle-right"></i>Social Media
                                 Marketing</a></li>
                     </ul>
                 </div>

                 <div class="twc-news-col-2">
                     <h3 class="twc-footer-title">API</h3>
                     <ul class="twc-news-list">
                         <li><a href="sms-otp"><i class="fa-solid fa-angle-right"></i>SMS/OTP</a></li>
                         <li><a href="whatsapp"><i class="fa-solid fa-angle-right"></i>WhatsApp</a></li>
                         <li><a href="email-form"><i class="fa-solid fa-angle-right"></i>Email Form</a></li>
                         <li><a href="chatbot"><i class="fa-solid fa-angle-right"></i>Chat Bot</a></li>
                         <li><a href="google-forms"><i class="fa-solid fa-angle-right"></i>Google Forms</a></li>
                         <li><a href="payment-gateway"><i class="fa-solid fa-angle-right"></i>Payment Gateway</a>
                         </li>
                     </ul>
                 </div>

                 <div class="twc-contact-col">
                     <h3 class="twc-footer-title">Contact Info</h3>
                     <ul class="twc-contact-list">
                         <li>
                             <i class="fa-solid fa-location-dot"></i>
                             <span>
                                 Address : Office No. 501, 5th Floor, Ghanshyam Enclave, New Link Road, Kandivali
                                 (West), Mumbai - 400067. Maharashtra - INDIA.
                             </span>
                         </li>
                         <!-- <li>
                             <i class="fa-solid fa-location-dot"></i>
                             <span>
                                 Address : 96 Parore Street, Dargaville 0310, New Zealand.
                             </span>
                         </li> -->
                         <li>
                             <i class="fa-solid fa-phone"></i>
                             <span>
                                 <a href="tel:+918080803374"> +91 8080 80 3374</a><br>
                                 <a href="tel:+918080803375"> +91 8080 80 3375</a> <br>
                                 <!-- <a href="tel:+64225055648"> New Zealand : +64 22 505 5648</a> -->
                             </span>
                         </li>
                         <li>
                             <i class="fa-solid fa-envelope"></i>
                             <span>
                                 <a href="mailto:info@technofra.com">info@technofra.com</a><br>
                                 <a href="mailto:support@technofra.com">support@technofra.com</a>
                                 <!-- <a href="mailto:info@technofra.co.nz">info@technofra.co.nz</a> -->
                             </span>
                         </li>
                         <li>
                             <i class="fa-solid fa-globe"></i>
                             <span> <a href="https://technofra.com/#">www.technofra.com</a> </span>
                         </li>
                         <!--<li>
                             <i class="fa-brands fa-x-twitter"></i>
                             <span><a href="https://x.com/Technofra_">twitter.com/Technofra</a></span>
                         </li> -->
                     </ul>
                 </div>

             </div>
         </div>

         <div class="twc-footer-middle">
             <div class="twc-apps-row">
                 <!-- <div class="twc-available-text">COMING SOON</div> -->

                 <a href="app" target="_blank" class="APPS" contenteditable="false" style="cursor: pointer; ">

                     <img src="assets/image/home/app0.webp" alt="Technofra app" class="lgs">
                 </a>

                 <a href="app" target="_blank" class="APPS" contenteditable="false" style="cursor: pointer;">
                     <img src="assets/image/home/ios0.webp" alt="Technofra app" class="lgs">
                 </a>

                 <div class="twc-socials">
                     <a href="https://hk.linkedin.com/company/technofra" target="_blank"><i
                             class="fa-brands fa-linkedin-in"></i></a>
                     <a href="https://x.com/Technofra_" target="_blank"><i class="fa-brands fa-x-twitter"></i></a>
                     <a href="https://www.facebook.com/Technofra/" target="_blank"><i class="fab fa-facebook-f"></i></a>
                     <a href="https://www.instagram.com/technofra.company/" target="_blank"><i
                             class="fa-brands fa-instagram"></i></a>
                 </div>
             </div>

             <div class="twc-brands-grid">
                 <div class="twc-brand-group-left">
                     <h3 class="twc-brand-group-title">Our Partners</h3>
                     <div class="twc-logos-row">
                         <div class="twc-logo-item">
                             <img src="assets/image/home/office356.webp" alt="Office365" class="footer-image">
                         </div>
                         <div class="twc-logo-item">
                             <img src="assets/image/home/payumoney.webp" alt="PayuMoney" class="footer-image">
                         </div>
                         <div class="twc-logo-item">
                             <img src="assets/image/home/razorpay.webp" alt="Razorpay" class="footer-image">
                         </div>
                         <div class="twc-logo-item">
                             <img src="assets/image/home/gridmce.webp" alt="Gridmce" class="footer-image">
                         </div>
                     </div>
                 </div>

                 <div class="twc-v-divider"></div>

                 <div class="twc-brand-group-right">
                     <h3 class="twc-brand-group-title">Our Brands</h3>
                     <div class="twc-logos-row">
                         <div class="twc-logo-item">
                             <a href="https://blueorbith.com/" target="_blank">
                                 <img src="assets/image/home/blueorbith.png" alt="Blue Orbith" class="footer-image">
                             </a>
                         </div>

                         <div class="twc-logo-item">
                             <a href="https://www.gridinfinity.com/" target="_blank">
                                 <img src="assets/image/home/grid-infinity.png" alt="Grid Infinity"
                                     class="footer-image">
                             </a>
                         </div>

                         <div class="twc-logo-item">
                             <a href="https://digikcon.com/" target="_blank">
                                 <img src="assets/image/home/digikcon.png" alt="Digikcon" class="footer-image">
                             </a>
                         </div>

                         <div class="twc-logo-item">
                             <a href="https://markidentitiez.com/" target="_blank">
                                 <img src="assets/image/home/markidentitiez.png" alt="Mark Identitiez"
                                     class="footer-image">
                             </a>
                         </div>
                     </div>
                 </div>
             </div>
         </div>

         <div class="twc-footer-bottom">
             <div class="twc-bottom-links">
                 <a href="about">About Us </a>
                 <a href="contact">Contact Us</a>
                 <a href="portfolio">Portfolio</a>
                 <a href="privacy-policy">Privacy Policy</a>
                 <a href="refund-policy">Refund Policy</a>
                 <a href="sitemap.html">Sitemap</a>
             </div>

             <div class="twc-copyright">
                 <span>©
                     <?php echo date("Y"); ?>
                 </span> <a href='https://technofra.com/'>Technofra</a> . All Rights Reserved.</p>
             </div>

             <!-- <div class="twc-floating-shape"></div> -->
         </div>

     </div>

 </footer>

 <div class="modal fade company-profile-modal" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false"
     tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
     <div class="modal-dialog modal-dialog-centered modal-lg">
         <div class="modal-content">
             <div class="modal-header">
                 <h1 class="modal-title fs-5" id="staticBackdropLabel">Download Company Profile</h1>
                 <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
             </div>
             <div
                 class="modal-body register-wrap p-5 bg-white shadow rounded-custom position-relative aos-init aos-animate">
                 <p>Fill in your details and we’ll email you the company profile.</p>
                 <!-- Job Application Form -->
                 <form action="send7" method="post" enctype="multipart/form-data" id="companyProfileForm">
                     <div class="company-profile-form-shell">
                         <!-- Full Name -->
                         <div class="company-profile-field text-start">
                             <label for="name" class="form-label">Full Name*</label>
                             <input type="text" class="form-control ca-two-border" name="name" id="name" required>
                             <span id="name-error" style="color: red; display: none;">Please
                                 enter a valid name (letters only)</span>
                         </div>

                         <!-- Email -->
                         <div class="company-profile-field text-start">
                             <label for="email" class="form-label">Email ID*</label>
                             <input type="email" class="form-control ca-two-border" name="email" id="email" required>
                             <span id="email-error" style="color: red; display: none;">Please
                                 enter a valid email address</span>
                         </div>

                         <!-- Contact Details -->
                         <div class="company-profile-field text-start">
                             <label for="phone" class="form-label">Contact Details*</label>
                             <input type="tel" class="form-control ca-two-border" name="contact" id="phone" required>
                             <span id="phone-error" style="color: red; display: none;">Please
                                 enter a valid phone number (10 digits)</span>
                         </div>
                         <!-- Location -->



                         <input type="text" name="hidden_field" style="display:none;" tabindex="-1">
                         <div class="company-profile-captcha">
                             <div class="g-recaptcha" data-sitekey="6LekpbEqAAAAANkc3FduPE52-p4Wqu5ghQFXjPhF"></div>
                         </div>
                         <!-- Submit Button -->
                         <div class="company-profile-actions text-start">
                             <button type="submit" class="mt-3 btn btn-outline-info company-profile-submit-btn"
                                 id="companyProfileSubmitBtn" data-default-text="Download Company Profile"
                                 data-loading-text="Sending...">
                                 Download Company Profile
                             </button>
                         </div>
                     </div>
                 </form>
             </div>
         </div>
     </div>
 </div>
 <!-- Floating Chatbot -->
 <?php $tfBasePath = rtrim(str_replace('\\', '/', dirname($_SERVER['SCRIPT_NAME'] ?? '')), '/.'); ?>
 <div class="tf-chatbot-wrap"
     data-page="<?php echo htmlspecialchars(parse_url($_SERVER['REQUEST_URI'] ?? 'index', PHP_URL_PATH) ?: 'index', ENT_QUOTES, 'UTF-8'); ?>"
     data-endpoint="<?php echo htmlspecialchars(($tfBasePath !== '' ? $tfBasePath : '') . '/chatbot-handler', ENT_QUOTES, 'UTF-8'); ?>">
     <button class="tf-chatbot-toggle" id="tfChatbotToggle" aria-label="Open chat">
         <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
             <path
                 d="M8 10.5h8M8 14h5M12 3C6.477 3 2 6.925 2 11.765c0 2.066.819 3.965 2.188 5.48L3.2 21l4.207-1.59A11.18 11.18 0 0 0 12 20.53c5.523 0 10-3.925 10-8.765S17.523 3 12 3Z"
                 stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
         </svg>
     </button>

     <div class="tf-chatbot-box" id="tfChatbotBox">
         <div class="tf-chatbot-header">
             <div>
                 <h4>Technofra Support</h4>
                 <span>AI support assistant</span>
             </div>
             <button class="tf-chatbot-close" id="tfChatbotClose" aria-label="Close chat">×</button>
         </div>

         <div class="tf-chatbot-body" id="tfChatbotBody">
             <div class="tf-msg tf-msg-bot">
                 Hi 👋<br>
                 I am Technofra's AI assistant. Please share your requirement, and I will guide you with the most relevant next step.
             </div>
             <div class="tf-msg tf-msg-bot">
                 You can ask about website development, app development, branding, UI/UX, payment integration, or chatbot solutions.
             </div>
             <div class="tf-chatbot-quick-actions" id="tfChatbotQuickActions">
                 <button type="button" class="tf-chatbot-chip" data-prompt="I need help with website development.">Website Project</button>
                 <button type="button" class="tf-chatbot-chip" data-prompt="I want to discuss app development requirements.">App Development</button>
                 <button type="button" class="tf-chatbot-chip" data-prompt="Please share your pricing process.">Pricing</button>
                 <button type="button" class="tf-chatbot-chip" data-prompt="I want your contact details.">Contact Team</button>
             </div>
         </div>

         <div class="tf-chatbot-footer">
             <input type="text" id="tfChatbotInput" placeholder="Type your message..." autocomplete="off" />
             <button id="tfChatbotSend" type="button">Send</button>
         </div>
     </div>
 </div>

 </div>
 <a href="https://wa.me/918080721003" class="whatsapp-float" target="_blank" rel="noopener noreferrer"
     aria-label="Chat on WhatsApp">
     <img src="https://upload.wikimedia.org/wikipedia/commons/6/6b/WhatsApp.svg" alt="WhatsApp" />
 </a>

 <script>
(function(w, d) {
    const captchaNodes = Array.from(d.querySelectorAll('.g-recaptcha'));

    if (!captchaNodes.length) {
        return;
    }

    let recaptchaLoaded = false;

    function loadRecaptcha() {
        if (recaptchaLoaded) {
            return;
        }

        recaptchaLoaded = true;
        const script = d.createElement('script');
        script.src = 'https://www.google.com/recaptcha/api.js';
        script.async = true;
        script.defer = true;
        d.head.appendChild(script);
    }

    if ('IntersectionObserver' in w) {
        const observer = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
                if (entry.isIntersecting) {
                    loadRecaptcha();
                    observer.disconnect();
                }
            });
        }, {
            rootMargin: '300px 0px'
        });

        captchaNodes.forEach((node) => observer.observe(node));
    }

    d.addEventListener('focusin', function(event) {
        if (event.target.closest('.company-profile-modal, form, .g-recaptcha')) {
            loadRecaptcha();
        }
    }, {
        once: true
    });

    d.addEventListener('pointerdown', function(event) {
        if (event.target.closest('[data-bs-target], .company-profile-modal, form, .g-recaptcha')) {
            loadRecaptcha();
        }
    }, {
        once: true,
        passive: true
    });

})(window, document);
 </script>

 <script>
window.addEventListener("load", function() {
    const track = document.getElementById("medxSliderTrack");
    const phoneBg = document.getElementById("phoneBg");

    if (!track || !phoneBg) return;

    const originalCards = Array.from(track.children);
    const originalCount = originalCards.length;
    const gap = 16;

    if (!originalCount) return;

    const firstCard = originalCards[0];

    if (!firstCard) {
        return;
    }

    const cardWidth = firstCard.getBoundingClientRect().width + gap;

    if (!cardWidth) {
        return;
    }

    const cloneFragment = document.createDocumentFragment();
    originalCards.forEach(card => cloneFragment.appendChild(card.cloneNode(true)));
    originalCards.forEach(card => cloneFragment.appendChild(card.cloneNode(true)));
    track.appendChild(cloneFragment);
    track.style.willChange = "transform";

    let position = originalCount;
    let lastIndex = -1;
    let animationFrameId = 0;
    let isAnimating = false;

    const speed = 0.006;
    const changeStartPoint = 0.18;

    function updatePhoneBackground(activeIndex) {
        if (activeIndex === lastIndex) {
            return;
        }

        const activeImg = originalCards[activeIndex] && originalCards[activeIndex].querySelector("img");
        if (activeImg && phoneBg.src !== activeImg.src) {
            phoneBg.src = activeImg.src;
        }

        lastIndex = activeIndex;
    }

    function animateSlider() {
        if (!isAnimating) {
            return;
        }

        position += speed;

        if (position >= originalCount * 2) {
            position = originalCount;
        }

        track.style.transform = `translateX(-${position * cardWidth}px)`;

        const integerPart = Math.floor(position);
        const decimalPart = position - integerPart;
        const activeIndex = decimalPart >= changeStartPoint ?
            (integerPart + 1) % originalCount :
            integerPart % originalCount;

        updatePhoneBackground(activeIndex);
        animationFrameId = requestAnimationFrame(animateSlider);
    }

    function startSlider() {
        if (isAnimating || document.hidden) {
            return;
        }

        isAnimating = true;
        animationFrameId = requestAnimationFrame(animateSlider);
    }

    function stopSlider() {
        if (!isAnimating) {
            return;
        }

        isAnimating = false;
        cancelAnimationFrame(animationFrameId);
    }

    track.style.transform = `translateX(-${position * cardWidth}px)`;
    updatePhoneBackground(0);
    startSlider();

    document.addEventListener("visibilitychange", function() {
        if (document.hidden) {
            stopSlider();
        } else {
            startSlider();
        }
    });

    window.addEventListener("pagehide", stopSlider, {
        once: true
    });
});
 </script>

 <!-- Google tag (gtag.js) -->
 <script>
(function(w, d, ids) {
    w.__techGtagIds = w.__techGtagIds || [];

    ids.forEach(function(id) {
        if (w.__techGtagIds.indexOf(id) === -1) {
            w.__techGtagIds.push(id);
        }
    });

    function ensureConfigs() {
        w.dataLayer = w.dataLayer || [];
        w.gtag = w.gtag || function() {
            w.dataLayer.push(arguments);
        };

        if (!w.__techGtagBootstrapped) {
            w.__techGtagBootstrapped = true;
            w.gtag('js', new Date());
        }

        w.__techGtagConfigured = w.__techGtagConfigured || {};
        w.__techGtagIds.forEach(function(id) {
            if (!w.__techGtagConfigured[id]) {
                w.gtag('config', id);
                w.__techGtagConfigured[id] = true;
            }
        });
    }

    function loadGtag() {
        ensureConfigs();

        if (w.__techGtagScriptLoading || w.__techGtagScriptLoaded) {
            return;
        }

        w.__techGtagScriptLoading = true;

        const script = d.createElement('script');
        script.async = true;
        script.src = 'https://www.googletagmanager.com/gtag/js?id=' + encodeURIComponent(w.__techGtagIds[0]);
        script.onload = function() {
            w.__techGtagScriptLoading = false;
            w.__techGtagScriptLoaded = true;
            ensureConfigs();
        };
        script.onerror = function() {
            w.__techGtagScriptLoading = false;
        };
        d.head.appendChild(script);
    }

    function scheduleGtagLoad() {
        if ('requestIdleCallback' in w) {
            requestIdleCallback(loadGtag, {
                timeout: 3500
            });
        } else {
            setTimeout(loadGtag, 1800);
        }
    }

    w.addEventListener('load', scheduleGtagLoad, {
        once: true
    });

    d.addEventListener('pointerdown', loadGtag, {
        once: true,
        passive: true,
        capture: true
    });

    d.addEventListener('focusin', loadGtag, {
        once: true,
        capture: true
    });
})(window, document, ['G-7DCEZBZGJN']);
 </script>

 <script>
const navToggle = document.querySelector('.navbar-toggler');
const navClose = document.querySelector('.close-btn, .btn-close');
const rightMenu = document.getElementById('rightMenu');

if (navToggle && rightMenu) {
    navToggle.addEventListener('click', () => {
        rightMenu.classList.toggle('active');
    });
}

if (navClose && rightMenu) {
    navClose.addEventListener('click', () => {
        rightMenu.classList.remove('active');
    });
}
 </script>

 <script>
(function() {
    const widget = document.querySelector(".tf-chatbot-wrap");
    const toggleBtn = document.getElementById("tfChatbotToggle");
    const closeBtn = document.getElementById("tfChatbotClose");
    const chatBox = document.getElementById("tfChatbotBox");
    const sendBtn = document.getElementById("tfChatbotSend");
    const input = document.getElementById("tfChatbotInput");
    const body = document.getElementById("tfChatbotBody");
    const quickActions = document.getElementById("tfChatbotQuickActions");

    if (!widget || !toggleBtn || !closeBtn || !chatBox || !sendBtn || !input || !body) {
        return;
    }

    const endpoint = widget.dataset.endpoint || "chatbot-handler.php";
    const currentPage = widget.dataset.page || window.location.pathname;
    const defaultButtonText = sendBtn.textContent;
    const introMessages = body.querySelectorAll(".tf-msg-bot");
    let historyLoaded = false;
    let isSending = false;

    function scrollBodyToBottom() {
        requestAnimationFrame(function() {
            body.scrollTop = body.scrollHeight;
        });
    }

    closeBtn.innerHTML = "&times;";

    if (introMessages.length > 0) {
        introMessages[0].innerHTML = "Hello.<br>I am Technofra's AI assistant. Please share your requirement, and I will guide you with the most relevant next step.";
    }

    function toggleChat(forceOpen) {
        if (typeof forceOpen === "boolean") {
            chatBox.classList.toggle("active", forceOpen);
        } else {
            chatBox.classList.toggle("active");
        }

        if (chatBox.classList.contains("active")) {
            setTimeout(function() {
                input.focus();
            }, 150);
        }
    }

    function appendMessage(text, type, allowHtml) {
        const message = document.createElement("div");
        message.className = "tf-msg " + (type === "user" ? "tf-msg-user" : "tf-msg-bot");

        if (allowHtml) {
            message.innerHTML = text;
        } else {
            message.textContent = text;
        }

        body.appendChild(message);
        scrollBodyToBottom();
        return message;
    }

    function createTypingMessage() {
        const typingMessage = document.createElement("div");
        typingMessage.className = "tf-msg tf-msg-bot tf-msg-typing";
        typingMessage.innerHTML = "<span></span><span></span><span></span>";
        body.appendChild(typingMessage);
        scrollBodyToBottom();
        return typingMessage;
    }

    function setSendingState(state) {
        isSending = state;
        input.disabled = state;
        sendBtn.disabled = state;
        sendBtn.textContent = state ? "Sending..." : defaultButtonText;
    }

    async function requestChat(payload) {
        const formData = new URLSearchParams();

        Object.keys(payload).forEach(function(key) {
            formData.append(key, payload[key]);
        });

        const response = await fetch(endpoint, {
            method: "POST",
            credentials: "same-origin",
            headers: {
                "Content-Type": "application/x-www-form-urlencoded; charset=UTF-8",
                "Accept": "application/json"
            },
            body: formData.toString()
        });

        const responseText = await response.text();
        let data = null;

        try {
            data = JSON.parse(responseText);
        } catch (error) {
            console.error("Chatbot endpoint returned non-JSON response:", responseText);
            throw new Error("Invalid server response.");
        }

        if (!response.ok || !data.success) {
            throw new Error(data.message || "Chat request failed.");
        }

        return data;
    }

    async function loadHistory() {
        if (historyLoaded) {
            return;
        }

        historyLoaded = true;

        try {
            const data = await requestChat({
                action: "history"
            });

            if (!Array.isArray(data.history) || !data.history.length) {
                return;
            }

            body.innerHTML = "";

            data.history.forEach(function(item) {
                appendMessage(item.html || "", item.role === "user" ? "user" : "bot", true);
            });
        } catch (error) {
            historyLoaded = false;
        }
    }

    async function sendMessage() {
        const text = input.value.trim();

        if (!text || isSending) {
            return;
        }

        appendMessage(text, "user", false);
        input.value = "";

        const typingMessage = createTypingMessage();
        setSendingState(true);

        try {
            const data = await requestChat({
                action: "message",
                message: text,
                page: currentPage
            });

            typingMessage.remove();
            appendMessage(data.reply || "Thanks for your message. Our team will get back to you shortly.",
                "bot", true);
        } catch (error) {
            typingMessage.remove();
            appendMessage(
                'Sorry, live chat is temporarily unavailable.<br>Please use <a href="https://wa.me/918080721003" target="_blank" rel="noopener noreferrer">WhatsApp</a> or our <a href="contact">contact page</a>.',
                "bot",
                true
            );
        } finally {
            setSendingState(false);
            input.focus();
            scrollBodyToBottom();
        }
    }

    toggleBtn.addEventListener("click", async function() {
        const willOpen = !chatBox.classList.contains("active");
        toggleChat(willOpen);

        if (willOpen) {
            await loadHistory();
        }
    });

    closeBtn.addEventListener("click", function() {
        toggleChat(false);
    });

    sendBtn.addEventListener("click", sendMessage);

    if (quickActions) {
        quickActions.addEventListener("click", function(event) {
            const chip = event.target.closest(".tf-chatbot-chip");

            if (!chip || isSending) {
                return;
            }

            input.value = chip.dataset.prompt || "";
            sendMessage();
        });
    }

    input.addEventListener("keydown", function(e) {
        if (e.key === "Enter") {
            e.preventDefault();
            sendMessage();
        }
    });

    document.addEventListener("click", function(e) {
        const insideWidget = e.target.closest(".tf-chatbot-wrap");

        if (!insideWidget && chatBox.classList.contains("active")) {
            toggleChat(false);
        }
    });
})();
 </script>

 <script>
/* Removed duplicate chatbot fallback block that never executes.
    const toggleBtn = document.getElementById("tfChatbotToggle");
    const closeBtn = document.getElementById("tfChatbotClose");
    const chatBox = document.getElementById("tfChatbotBox");
    const sendBtn = document.getElementById("tfChatbotSend");
    const input = document.getElementById("tfChatbotInput");
    const body = document.getElementById("tfChatbotBody");

    function toggleChat() {
        chatBox.classList.toggle("active");
        if (chatBox.classList.contains("active")) {
            setTimeout(() => input.focus(), 150);
        }
    }

    function appendMessage(text, type) {
        const msg = document.createElement("div");
        msg.className = "tf-msg " + (type === "user" ? "tf-msg-user" : "tf-msg-bot");
        msg.innerHTML = text;
        body.appendChild(msg);
        body.scrollTop = body.scrollHeight;
    }

    function botReply(userText) {
        let reply = "Thanks for your message. Our team will get back to you shortly.";

        const text = userText.toLowerCase();

        if (text.includes("website")) {
            reply = "We can help with Website Development, WordPress, Shopify, and custom business websites.";
        } else if (text.includes("app")) {
            reply = "We also build Android and iOS applications tailored to business needs.";
        } else if (text.includes("branding") || text.includes("ui") || text.includes("ux")) {
            reply = "Sure — we can help with Branding, UI/UX, and digital experience design.";
        } else if (text.includes("payment")) {
            reply = "We provide secure payment gateway integration for websites and apps.";
        } else if (text.includes("chatbot") || text.includes("bot")) {
            reply = "Yes, Technofra-style chatbot integration is available for your site.";
        }

        setTimeout(() => appendMessage(reply, "bot"), 500);
    }

    function sendMessage() {
        const text = input.value.trim();
        if (!text) return;
        appendMessage(text, "user");
        input.value = "";
        botReply(text);
    }

    toggleBtn.addEventListener("click", toggleChat);
    closeBtn.addEventListener("click", toggleChat);
    sendBtn.addEventListener("click", sendMessage);

    input.addEventListener("keydown", function(e) {
        if (e.key === "Enter") sendMessage();
    });

    document.addEventListener("click", function(e) {
        const insideWidget = e.target.closest(".tf-chatbot-wrap");
        if (!insideWidget && chatBox.classList.contains("active")) {
            chatBox.classList.remove("active");
        }
    });
*/
 </script>

 <script>
(function() {
    const track = document.getElementById('tfxIndusTrack');
    const slides = document.querySelectorAll('.tfxIndusSlide');
    const viewport = document.querySelector('.tfxIndusViewport');
    const prevBtn = document.querySelector('.tfxIndusPrev');
    const nextBtn = document.querySelector('.tfxIndusNext');
    const dots = document.querySelectorAll('.tfxIndusDot');

    if (!track || !slides.length || !viewport || !prevBtn || !nextBtn || !dots.length) {
        return;
    }

    let currentIndex = 1;
    let isAnimating = false;
    let autoSlide;
    const transitionDuration = 1.2;
    const autoSlideDelay = 5500;

    const totalSlides = slides.length;

    function updateDots() {
        dots.forEach(dot => dot.classList.remove('active'));
        if (currentIndex === 1) dots[0].classList.add('active');
        else if (currentIndex === 2) dots[1].classList.add('active');
    }

    function moveToSlide() {
        track.style.transition = `transform ${transitionDuration}s ease-in-out`;
        track.style.transform = `translateX(-${currentIndex * 100}%)`;
        updateDots();
    }

    function startAutoSlide() {
        clearInterval(autoSlide);
        autoSlide = setInterval(() => {
            goNext();
        }, autoSlideDelay);
    }

    function stopAutoSlide() {
        clearInterval(autoSlide);
    }

    function resetAutoSlide() {
        stopAutoSlide();
        startAutoSlide();
    }

    function goNext() {
        if (isAnimating) return;
        isAnimating = true;
        currentIndex++;
        moveToSlide();
    }

    function goPrev() {
        if (isAnimating) return;
        isAnimating = true;
        currentIndex--;
        moveToSlide();
    }

    track.addEventListener('transitionend', function() {
        if (currentIndex === totalSlides - 1) {
            track.style.transition = 'none';
            currentIndex = 1;
            track.style.transform = `translateX(-${currentIndex * 100}%)`;
        }

        if (currentIndex === 0) {
            track.style.transition = 'none';
            currentIndex = totalSlides - 2;
            track.style.transform = `translateX(-${currentIndex * 100}%)`;
        }

        updateDots();
        isAnimating = false;
    });

    nextBtn.addEventListener('click', function() {
        goNext();
        resetAutoSlide();
    });

    prevBtn.addEventListener('click', function() {
        goPrev();
        resetAutoSlide();
    });

    dots.forEach(dot => {
        dot.addEventListener('click', function() {
            if (isAnimating) return;
            const targetIndex = parseInt(this.getAttribute('data-index'));
            currentIndex = targetIndex;
            moveToSlide();
            resetAutoSlide();
        });
    });

    viewport.addEventListener('mouseenter', stopAutoSlide);
    viewport.addEventListener('mouseleave', startAutoSlide);

    track.style.transform = `translateX(-${currentIndex * 100}%)`;
    updateDots();
    startAutoSlide();
})();
 </script>

 <style>
.company-profile-submit-btn.is-loading {
    opacity: 0.75;
    cursor: not-allowed;
    pointer-events: none;
}
 </style>

 <script>
(function() {
    const preloader = document.getElementById('preloader');

    if (!preloader) {
        return;
    }

    let hidden = false;

    function hidePreloader() {
        if (hidden) {
            return;
        }

        hidden = true;
        preloader.style.display = 'none';
        preloader.setAttribute('aria-hidden', 'true');
    }

    if (document.readyState === 'complete' || document.readyState === 'interactive') {
        requestAnimationFrame(hidePreloader);
    } else {
        document.addEventListener('DOMContentLoaded', function() {
            requestAnimationFrame(hidePreloader);
        }, {
            once: true
        });
    }

    window.addEventListener('load', hidePreloader, {
        once: true
    });

    setTimeout(hidePreloader, 1800);
})();
 </script>

 <script>
(function() {
    const companyProfileForm = document.getElementById('companyProfileForm');
    const companyProfileSubmitBtn = document.getElementById('companyProfileSubmitBtn');

    if (!companyProfileForm || !companyProfileSubmitBtn) {
        return;
    }

    function resetCompanyProfileButton() {
        companyProfileSubmitBtn.disabled = false;
        companyProfileSubmitBtn.classList.remove('is-loading');
        companyProfileSubmitBtn.innerHTML = companyProfileSubmitBtn.dataset.defaultText;
    }

    companyProfileForm.addEventListener('submit', function() {
        companyProfileSubmitBtn.disabled = true;
        companyProfileSubmitBtn.classList.add('is-loading');
        companyProfileSubmitBtn.textContent = companyProfileSubmitBtn.dataset.loadingText;
    });

    window.addEventListener('pageshow', resetCompanyProfileButton);
})();
 </script>

 <?php
 $loadBootstrapScript = $loadBootstrapScript ?? true;
 $loadSwiperVendor = $loadSwiperVendor ?? true;
 $loadMagnificVendor = $loadMagnificVendor ?? true;
 $loadParallaxVendor = $loadParallaxVendor ?? true;
 $loadAosVendor = $loadAosVendor ?? true;
 $loadMassonryVendor = $loadMassonryVendor ?? true;
 $loadAppBundle = $loadAppBundle ?? true;
 $loadLegacyThemeBundle = $loadLegacyThemeBundle ?? true;
 $loadJqueryVendor = $loadJqueryVendor ?? ($loadAppBundle || $loadLegacyThemeBundle || $loadMagnificVendor || $loadParallaxVendor || $loadMassonryVendor);
 ?>

 <!--build:js-->
 <?php if ($loadJqueryVendor): ?>
 <script defer src="assets_01/js/jquery-3-7-1.min.js"></script>
 <?php endif; ?>
 <?php if ($loadBootstrapScript): ?>
 <script defer src="assets/js/vendors/bootstrap.bundle.min.js"></script>
 <?php endif; ?>
 <?php if ($loadSwiperVendor): ?>
 <script defer src="assets/js/vendors/swiper-bundle.min.js"></script>
 <?php endif; ?>
 <?php if ($loadMagnificVendor): ?>
 <script defer src="assets/js/vendors/jquery.magnific-popup.min.js"></script>
 <?php endif; ?>
 <?php if ($loadParallaxVendor): ?>
 <script defer src="assets/js/vendors/parallax.min.js"></script>
 <?php endif; ?>
 <?php if ($loadAosVendor): ?>
 <script defer src="assets/js/vendors/aos.js"></script>
 <?php endif; ?>
 <?php if ($loadMassonryVendor): ?>
 <script defer src="assets/js/vendors/massonry.min.js"></script>
 <?php endif; ?>
 <?php if ($loadAppBundle): ?>
 <script defer src="assets/js/app.js"></script>
 <?php endif; ?>
 <!--endbuild-->
 <?php if ($loadLegacyThemeBundle): ?>
 <script defer src="assets_01/js/jquery.magnific-popup.js"></script>
 <script defer src="assets_01/js/owl.carousel.min.js"></script>
 <script defer src="assets_01/js/slick-slider.js"></script>
 <script defer src="assets_01/js/gsap.min.js"></script>
 <script defer src="assets_01/js/ScrollTrigger.min.js"></script>
 <script defer src="assets_01/js/Splitetext.js"></script>
 <script defer src="assets_01/js/text-animation.js"></script>
 <script defer src="assets_01/js/SmoothScroll.js"></script>
 <script defer src="assets_01/js/jquery.lineProgressbar.js"></script>
 <script defer src="assets_01/js/ripple-btn.js"></script>
 <script defer src="assets_01/js/main.js"></script>
 <?php endif; ?>
 </body>



 </html>
